<configuration>
	<!-- Disables Logback init status messages, comment it if you need to debug logback init -->
	<statusListener class="ch.qos.logback.core.status.NopStatusListener" />

	<contextListener class="ch.qos.logback.classic.jul.LevelChangePropagator"/>

	<appender name="STDOUT" class="ch.qos.logback.core.ConsoleAppender">
		<encoder>
			<pattern>[%d{yyyy-MM-dd'T'HH:mm:ss.SSS}] %-5level %-48.48thread %-64.64logger{64} %msg %ex%n</pattern>
	    </encoder>
	    <!-- Replace the above encoder with the following one if you'd like to generate logstash compatible log entries in the console (and/or copy it to other appenders)
		<encoder class="net.logstash.logback.encoder.LoggingEventCompositeJsonEncoder">
            <providers>
                <timestamp>
                    <fieldName>timestamp</fieldName>
                    <timeZone>UTC</timeZone>
                </timestamp>
                <loggerName>
                    <fieldName>logger</fieldName>
                </loggerName>
                <logLevel>
                    <fieldName>level</fieldName>
                </logLevel>
                <threadName>
                    <fieldName>thread</fieldName>
                </threadName>
                <nestedField>
                    <fieldName>mdc</fieldName>
                    <providers>
                        <mdc />
                    </providers>
                </nestedField>
                <stackTrace>
                    <fieldName>stackTrace</fieldName>
                     maxLength - limit the length of the stack trace 
                    <throwableConverter class="net.logstash.logback.stacktrace.ShortenedThrowableConverter">
                        <maxDepthPerThrowable>200</maxDepthPerThrowable>
                        <maxLength>14000</maxLength>
                        <rootCauseFirst>true</rootCauseFirst>
                    </throwableConverter>
                </stackTrace>
                <message />
                <throwableClassName>
                    <fieldName>exceptionClass</fieldName>
                </throwableClassName>
            </providers>
	    </encoder>
	    -->
	</appender>
	
	<appender name="LOG_FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
		<file>${so.home}/serviceability/logs/log.log</file>
		<rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
			<fileNamePattern>${so.home}/serviceability/logs/log_%d{yyyy-MM-dd}.log</fileNamePattern>
			<!-- keep 90 days' worth of history -->
      		<maxHistory>90</maxHistory>
		</rollingPolicy>
		<encoder class="ch.qos.logback.classic.encoder.PatternLayoutEncoder">
			<pattern>[%d{yyyy-MM-dd'T'HH:mm:ss.SSS}] %-5level %-48.48thread %-64.64logger{64} %msg %ex%n</pattern>
		</encoder>
	</appender>

	<logger level="INFO" additivity="false" name="org.osgi.service.log.LogService">
		<appender-ref ref="LOG_FILE" />
	</logger>
	
	<logger level="ERROR" additivity="false" name="org.eclipse.jetty.ee10.annotations.AnnotationParser" />
	
	<!-- Suppresses "ELK supports DataHasValue only partially" warnings -->
	<logger level="ERROR" additivity="false" name="org.semanticweb.elk.reasoner.indexing.hierarchy.IndexObjectConverter" />
	<!-- Suppresses "ELK does not support <axiom type>. Axiom ignored" warnings -->
	<logger level="ERROR" additivity="false" name="org.semanticweb.elk.reasoner.indexing.hierarchy.ChangeIndexingProcessor" />
	<!-- Suppresses errors thrown during "Jetty Shutdown" -->
	<logger level="OFF" additivity="false" name="org.eclipse.jetty.osgi.AbstractContextProvider" />
	<!-- Suppresses deprecation warnings thrown by the Elasticsearch REST client -->
	<logger level="ERROR" additivity="false" name="org.elasticsearch.client.RestClient" />

	<root level="INFO">
		<appender-ref ref="LOG_FILE" />
		<appender-ref ref="STDOUT" />
	</root>

</configuration> 