<?xml version="1.0"?>
<project
	xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd"
	xmlns="http://maven.apache.org/POM/4.0.0"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance">
	<modelVersion>4.0.0</modelVersion>
	<groupId>com.b2international.snowowl</groupId>
	<artifactId>snowowl-parent</artifactId>
	<version>10.1.1-SNAPSHOT</version>
	<packaging>pom</packaging>

	<properties>
		<project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
		<project.reporting.outputEncoding>UTF-8</project.reporting.outputEncoding>

		<!-- Java version to use for compile and runtime -->
		<java.version>25</java.version>

		<!-- Dependency versions for Tycho's target platform filter -->
		<elk.version>0.4.3</elk.version>
		<guava.versionRange>[33.4.0,34.0.0)</guava.versionRange>
		<jackson.annotations.version>2.21</jackson.annotations.version>
		<jackson.version>2.21.1</jackson.version>
		<logback.versionRange>[1.5.32,1.6.0)</logback.versionRange>
		<osgi.versionRange>[3.24.0,3.25.0)</osgi.versionRange>
		<slf4j.version>2.0.17</slf4j.version>
		
		<!-- Build tool/compiler versions -->
		<tycho.version>5.0.3</tycho.version>
		<jacoco.version>0.8.14</jacoco.version>
		
		<!-- Versioning -->
		<maven.build.timestamp.format>yyyyMMddHHmm</maven.build.timestamp.format>
		<forceContextQualifier>${maven.build.timestamp}</forceContextQualifier>
		
		<!-- Testing -->
		<testHeapSize>1g</testHeapSize>
		<testLogConfigFile>${basedir}/src/configuration/logback-maven.xml</testLogConfigFile>
		<testArgLine>${tycho.testArgLine} -Xms${testHeapSize} -Xmx${testHeapSize} -Dosgi.classloader.type=nonparallel -Djetty.home.bundle=org.eclipse.jetty.ee10.osgi.boot -Djetty.http.port=9090 -XX:+AlwaysPreTouch --add-opens java.base/java.lang.reflect=ALL-UNNAMED --add-opens java.base/java.lang=ALL-UNNAMED --add-opens java.base/java.util=ALL-UNNAMED --add-opens java.base/java.time=ALL-UNNAMED --add-opens java.base/sun.security.x509=ALL-UNNAMED -Dlogback.configurationFile=${testLogConfigFile}</testArgLine>

		<!-- Artifact distribution -->
		<maven.repository.id>nexus_deployment</maven.repository.id>
		<maven.releases.repository>https://artifacts.b2ihealthcare.com/repository/maven-releases</maven.releases.repository>
		<maven.snapshots.repository>https://artifacts.b2ihealthcare.com/repository/maven-snapshots</maven.snapshots.repository>

	</properties>

	<modules>
		<module>commons</module>
		<module>netty</module>
		<module>core</module>
		<module>snomed</module>
		<module>cis</module>
		<module>fhir</module>
		<module>tests</module>
		<module>releng</module>
		<module>dist</module>
	</modules>

	<build>
		<plugins>
			<plugin>
				<groupId>org.eclipse.tycho</groupId>
				<artifactId>tycho-maven-plugin</artifactId>
				<version>${tycho.version}</version>
				<extensions>true</extensions>
			</plugin>
			
			<plugin>
				<groupId>org.eclipse.tycho</groupId>
				<artifactId>tycho-packaging-plugin</artifactId>
				<configuration>
					<strictBinIncludes>false</strictBinIncludes>
					<strictVersions>false</strictVersions>
				</configuration>
			</plugin>
			<plugin>
				<groupId>org.eclipse.tycho</groupId>
				<artifactId>tycho-source-plugin</artifactId>
				<version>${tycho.version}</version>
				<executions>
					<execution>
						<id>plugin-source</id>
						<goals>
							<goal>plugin-source</goal>
						</goals>
					</execution>
				</executions>
			</plugin>
			<plugin>
				<groupId>org.eclipse.tycho</groupId>
				<artifactId>target-platform-configuration</artifactId>
				<version>${tycho.version}</version>
				<configuration>
					<dependency-resolution>
						<localArtifacts>ignore</localArtifacts>
					</dependency-resolution>
					<target>
						<artifact>
							<groupId>${project.groupId}</groupId>
							<artifactId>target-platform</artifactId>
							<version>${project.version}</version>
						</artifact>
					</target>
					<filters>
						<filter>
							<type>eclipse-plugin</type>
							<id>org.eclipse.osgi</id>
							<restrictTo>
								<versionRange>${osgi.versionRange}</versionRange>
							</restrictTo>
						</filter>
						<!-- work around Equinox bug 348045 -->
						<!-- Removes the extension bundle from the available bundles for the 
							javax.servlet package -->
						<filter>
							<type>p2-installable-unit</type>
							<id>org.eclipse.equinox.servletbridge.extensionbundle</id>
							<removeAll />
						</filter>
						<filter>
							<type>eclipse-plugin</type>
							<id>com.google.guava</id>
							<restrictTo>
								<versionRange>${guava.versionRange}</versionRange>
							</restrictTo>
						</filter>
						<!-- Restrict SLF4J version -->
						<filter>
							<type>p2-installable-unit</type>
							<id>slf4j.api</id>
							<restrictTo>
								<versionRange>${slf4j.version}</versionRange>
							</restrictTo>
						</filter>
						<filter>
							<type>p2-installable-unit</type>
							<id>ch.qos.logback.classic</id>
							<restrictTo>
								<versionRange>${logback.versionRange}</versionRange>
							</restrictTo>
						</filter>
						<filter>
							<type>p2-installable-unit</type>
							<id>ch.qos.logback.core</id>
							<restrictTo>
								<versionRange>${logback.versionRange}</versionRange>
							</restrictTo>
						</filter>
						<!-- Restrict Jackson bundles to the selected version -->
						<filter>
							<type>p2-installable-unit</type>
							<id>com.fasterxml.jackson.core.jackson-annotations</id>
							<restrictTo>
								<version>${jackson.annotations.version}</version>
							</restrictTo>
						</filter>
						<filter>
							<type>p2-installable-unit</type>
							<id>com.fasterxml.jackson.core.jackson-core</id>
							<restrictTo>
								<version>${jackson.version}</version>
							</restrictTo>
						</filter>
						<filter>
							<type>p2-installable-unit</type>
							<id>com.fasterxml.jackson.core.jackson-databind</id>
							<restrictTo>
								<version>${jackson.version}</version>
							</restrictTo>
						</filter>
						<filter>
							<type>p2-installable-unit</type>
							<id>com.fasterxml.jackson.dataformat.jackson-dataformat-yaml</id>
							<restrictTo>
								<version>${jackson.version}</version>
							</restrictTo>
						</filter>
						<filter>
							<type>p2-installable-unit</type>
							<id>com.fasterxml.jackson.dataformat.jackson-dataformat-cbor</id>
							<restrictTo>
								<version>${jackson.version}</version>
							</restrictTo>
						</filter>
						<filter>
							<type>p2-installable-unit</type>
							<id>com.fasterxml.jackson.dataformat.jackson-dataformat-csv</id>
							<restrictTo>
								<version>${jackson.version}</version>
							</restrictTo>
						</filter>
						<filter>
							<type>p2-installable-unit</type>
							<id>com.fasterxml.jackson.dataformat.jackson-dataformat-xml</id>
							<restrictTo>
								<version>${jackson.version}</version>
							</restrictTo>
						</filter>
						<filter>
							<type>p2-installable-unit</type>
							<id>com.fasterxml.jackson.dataformat.jackson-dataformat-afterburner</id>
							<restrictTo>
								<version>${jackson.version}</version>
							</restrictTo>
						</filter>
						<filter>
							<type>p2-installable-unit</type>
							<id>org.slf4j.api</id>
							<removeAll />
						</filter>
						<filter>
							<type>p2-installable-unit</type>
							<id>org.slf4j.jcl</id>
							<removeAll />
						</filter>
						<!--Remove log4j 1.x jars-->
						<filter>
							<type>p2-installable-unit</type>
							<id>org.apache.log4j</id>
							<removeAll />
						</filter>
					</filters>
					<environments>
						<environment>
							<os>linux</os>
							<ws>gtk</ws>
							<arch>x86_64</arch>
						</environment>
						<environment>
							<os>win32</os>
							<ws>win32</ws>
							<arch>x86_64</arch>
						</environment>
					</environments>
				</configuration>
			</plugin>
			<plugin>
				<groupId>org.jacoco</groupId>
				<artifactId>jacoco-maven-plugin</artifactId>
				<version>${jacoco.version}</version>
				<executions>
					<execution>
						<goals>
							<goal>prepare-agent</goal>
						</goals>
					</execution>
				</executions>
				<configuration>
					<excludes>
						<!-- Exclude large SNOMED language parser files -->
						<exclude>com/b2international/snowowl/snomed/etl/parser/**/*</exclude>
						<exclude>com/b2international/snowowl/snomed/ql/parser/**/*</exclude>
						<exclude>com/b2international/snowowl/snomed/scg/parser/**/*</exclude>
						<!-- Exclude anything that HL7 FHIR related -->
						<exclude>org/hl7/fhir/**/*</exclude>
					</excludes>
				</configuration>
			</plugin>
		</plugins>
		<pluginManagement>
			<plugins>
				<plugin>
					<groupId>org.eclipse.tycho</groupId>
					<artifactId>tycho-p2-plugin</artifactId>
					<version>${tycho.version}</version>
				</plugin>
				<plugin>
					<groupId>org.eclipse.tycho</groupId>
					<artifactId>tycho-p2-director-plugin</artifactId>
					<version>${tycho.version}</version>
					<configuration>
						<!-- Use the selected JustJ version repository to include the JRE -->
	    				<productRepository>https://download.eclipse.org/justj/jres/${java.version}/updates/release/</productRepository>
					</configuration>
				</plugin>
				<plugin>
					<groupId>org.eclipse.tycho</groupId>
					<artifactId>tycho-p2-publisher-plugin</artifactId>
					<version>${tycho.version}</version>
				</plugin>
				<plugin>
					<groupId>org.eclipse.tycho</groupId>
					<artifactId>tycho-versions-plugin</artifactId>
					<version>${tycho.version}</version>
				</plugin>
				<plugin>
					<groupId>org.eclipse.tycho</groupId>
					<artifactId>tycho-compiler-plugin</artifactId>
					<version>${tycho.version}</version>
					<configuration>
						<useProjectSettings>false</useProjectSettings>
						<release>${java.version}</release>
						<excludeResources>
							<excludeResource>**/*.groovy</excludeResource>
						</excludeResources>
					</configuration>
				</plugin>
				<!-- Maven compiler plugin definition needed for Groovy compiler -->
				<plugin>
					<groupId>org.apache.maven.plugins</groupId>
					<artifactId>maven-compiler-plugin</artifactId>
					<version>3.15.0</version>
					<configuration>
						<release>${java.version}</release>
					</configuration>
				</plugin>
				<plugin>
					<groupId>org.eclipse.tycho</groupId>
					<artifactId>tycho-surefire-plugin</artifactId>
					<version>${tycho.version}</version>
					<configuration>
						<useUIThread>false</useUIThread>
						<failIfNoTests>false</failIfNoTests>
						<trimStackTrace>false</trimStackTrace>
						<argLine>${testArgLine}</argLine>
						<forkedProcessTimeoutInSeconds>3600</forkedProcessTimeoutInSeconds>
					</configuration>
				</plugin>
				<plugin>
					<groupId>org.eclipse.tycho</groupId>
					<artifactId>tycho-packaging-plugin</artifactId>
					<version>${tycho.version}</version>
					<dependencies>
						<dependency>
							<groupId>org.eclipse.tycho</groupId>
							<artifactId>tycho-buildtimestamp-jgit</artifactId>
							<version>${tycho.version}</version>
						</dependency>
					</dependencies>
					<configuration>
						<timestampProvider>jgit</timestampProvider>
						<jgit.ignore>pom.xml</jgit.ignore>
						<jgit.dirtyWorkingTree>ignore</jgit.dirtyWorkingTree>
						<skipPomGeneration>true</skipPomGeneration>
					</configuration>
				</plugin>
				<plugin>
					<groupId>org.eclipse.tycho</groupId>
					<artifactId>tycho-p2-repository-plugin</artifactId>
					<version>${tycho.version}</version>
					<configuration>
						<includeAllDependencies>true</includeAllDependencies>
					</configuration>
				</plugin>
				<plugin>
					<groupId>org.codehaus.mojo</groupId>
					<artifactId>build-helper-maven-plugin</artifactId>
					<version>3.6.1</version>
				</plugin>
				<plugin>
					<groupId>org.apache.maven.plugins</groupId>
					<artifactId>maven-assembly-plugin</artifactId>
					<version>3.8.0</version>
				</plugin>
				<plugin>
					<groupId>org.apache.maven.plugins</groupId>
					<artifactId>maven-clean-plugin</artifactId>
					<version>3.5.0</version>
				</plugin>
				<plugin>
					<groupId>org.apache.maven.plugins</groupId>
					<artifactId>maven-resources-plugin</artifactId>
					<version>3.5.0</version>
				</plugin>
				<plugin>
					<groupId>org.apache.maven.plugins</groupId>
					<artifactId>maven-dependency-plugin</artifactId>
					<version>3.11.0</version>
				</plugin>
				<plugin>
					<artifactId>maven-install-plugin</artifactId>
					<version>3.1.4</version>
					<configuration>
						<installAtEnd>true</installAtEnd>
					</configuration>
				</plugin>
				<plugin>
					<artifactId>maven-deploy-plugin</artifactId>
					<version>3.1.4</version>
					<configuration>
						<deployAtEnd>true</deployAtEnd>
					</configuration>
				</plugin>
			</plugins>
		</pluginManagement>
	</build>

	<repositories>
		<!-- Specify central first, as most of our dependencies come from there, ensure that all extra repositories are defined with an id after the central id -->
		<repository>
	      <id>central</id>
	      <url>https://repo.maven.apache.org/maven2</url>
	      <name>Central Repository</name>
	      <releases>
	      	  <enabled>true</enabled>
	      </releases>
      	  <snapshots>
      	  	  <enabled>false</enabled>
	      </snapshots>
    	</repository>
		<!-- 
			B2i repositories defined here should allow resolving zipped update sites with an mvn: URL in the
			target platform definition, ie. the ECL, ETL and SCG features. All other sources are now 
			referenced directly in the TP file. 
		-->
		<repository>
			<id>maven-b2i-releases</id>
			<url>https://artifacts.b2ihealthcare.com/repository/maven-releases</url>
			<releases>
				<enabled>true</enabled>
			</releases>
			<snapshots>
				<enabled>false</enabled>
			</snapshots>
		</repository>
		<repository>
			<id>maven-b2i-snapshots</id>
			<url>https://artifacts.b2ihealthcare.com/repository/maven-snapshots</url>
			<releases>
				<enabled>false</enabled>
			</releases>
			<snapshots>
				<enabled>true</enabled>
				<updatePolicy>always</updatePolicy>
			</snapshots>
		</repository>
	</repositories>
			
	<distributionManagement>
		<repository>
			<id>${maven.repository.id}</id>
			<url>${maven.releases.repository}</url>
		</repository>
		<snapshotRepository>
			<id>${maven.repository.id}</id>
			<url>${maven.snapshots.repository}</url>
		</snapshotRepository>
	</distributionManagement>

	<scm>
		<url>https://github.com/b2ihealthcare/snow-owl</url>
		<connection>scm:git:ssh://github.com/b2ihealthcare/snow-owl.git</connection>
		<developerConnection>scm:git:ssh@github.com:b2ihealthcare/snow-owl.git</developerConnection>
	</scm>
	
</project>
